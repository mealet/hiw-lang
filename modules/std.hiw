define range(range_end) {
  for i in range_end + 1 {
    op!(FETCH i);
  };

  op!(ARR);
};

define type(arg) {
  op!(FETCH arg TYPE);
};

define to_int(arg) {
  op!(FETCH arg TO_INT);
};

define to_str(arg) {
  op!(FETCH arg TO_STR);
};
